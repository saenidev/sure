<% content_for :title, t("forecasts.reconciliation.index.title") %>

<div class="space-y-6 pb-6 lg:pb-12">
  <header class="space-y-1">
    <h1 class="text-primary text-2xl font-semibold"><%= t("forecasts.reconciliation.index.title") %></h1>
    <p class="text-secondary text-sm"><%= t("forecasts.reconciliation.description") %></p>
  </header>

  <%= render "forecasts/reconciliation", workspace: @workspace %>
</div>
