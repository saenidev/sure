<%# locals: (plan:) %>
<%# JSON island read via textContent and replaced by Turbo Stream on save. %>
<%# Rails escaping is intentional; the browser decodes entities for textContent. %>
<div id="forecast-spike-data" hidden
     data-forecast-spike-target="data"><%= plan.periods.to_json %></div>
